// run the examples using node conditionals.js
// try changing the code a bit - udpate values, reorder statements, to see how the output changes. '
// remember that you can create a file from the Terminal using the touch command, as in "> touch conditionals.js"

//Conditionals Execution 
const raining = true;
const cold = false;

if (raining) {
  console.log("Don't forget your umbrella!");
}

if (cold) {
  console.log("Make sure you pick out a scarf!");
}

console.log("Now you're ready to go outside!");